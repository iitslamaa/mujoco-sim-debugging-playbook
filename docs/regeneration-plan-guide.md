# Regeneration Plan Guide

The regeneration plan turns stale-artifact detection into a to-do list.

It answers:

- which outputs need refresh first
- which command is most likely to regenerate each output
- which stale artifacts should be treated as highest priority
