# Docker Context Guide

Use the Docker context report to capture the detected Docker CLI state and summarize the current Dockerfile.

```bash
python scripts/generate_docker_context.py
```

Outputs:

- `outputs/docker_context/docker_context.json`
- `outputs/docker_context/docker_context.md`
