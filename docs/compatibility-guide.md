# Compatibility Guide

Use the compatibility report to capture whether the current environment matches the repo's expected Python and tooling surface.

```bash
python scripts/generate_compatibility_report.py
```

Outputs:

- `outputs/compatibility/compatibility.json`
- `outputs/compatibility/compatibility.md`
