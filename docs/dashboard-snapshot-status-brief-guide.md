# Dashboard Snapshot Status Brief Guide

Run the status brief generator to combine the current priority stack and monitor headlines into one compact update.

```bash
python scripts/generate_dashboard_snapshot_status_brief.py
```

Outputs:

- `outputs/dashboard_snapshots/status_brief.json`
- `outputs/dashboard_snapshots/status_brief.md`
