# Evidence Inventory Guide

The evidence inventory is a raw catalog of generated artifacts.

Use it when you need to answer:

- what outputs already exist
- which output areas are actually populated
- whether an artifact was generated at all before debugging the content
