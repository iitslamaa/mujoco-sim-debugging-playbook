# Dashboard Snapshot Watchlist Guide

Run the watchlist generator to combine the most important dashboard snapshot alerts and actions into one compact operator queue.

```bash
python scripts/generate_dashboard_snapshot_watchlist.py
```

Outputs:

- `outputs/dashboard_snapshots/watchlist.json`
- `outputs/dashboard_snapshots/watchlist.md`
