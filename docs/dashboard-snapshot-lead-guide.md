# Dashboard Snapshot Lead Guide

Run the lead generator to turn the current status brief and review state into one short situation report.

```bash
python scripts/generate_dashboard_snapshot_lead.py
```

Outputs:

- `outputs/dashboard_snapshots/lead.json`
- `outputs/dashboard_snapshots/lead.md`
