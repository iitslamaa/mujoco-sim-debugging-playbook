# Dashboard Snapshot Focus Guide

Run the focus generator to condense the watchlist, readiness gate, and milestone path into one concise operator summary.

```bash
python scripts/generate_dashboard_snapshot_focus.py
```

Outputs:

- `outputs/dashboard_snapshots/focus.json`
- `outputs/dashboard_snapshots/focus.md`
