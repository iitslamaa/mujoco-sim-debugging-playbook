# Toolchain Inventory Guide

Use the toolchain inventory to record the main local runtime and CLI versions captured in the environment report.

```bash
python scripts/generate_toolchain_inventory.py
```
