# Debug Bundle Guide

Use the debug-bundle collector to package the most useful local support and diagnostics artifacts into one timestamped directory.

```bash
bash scripts/collect_debug_bundle.sh
```

Outputs land under `outputs/debug_bundle/<timestamp>/`.
