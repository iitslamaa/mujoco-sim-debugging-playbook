# Artifact Freshness Guide

The artifact-freshness audit checks whether key generated outputs still look current relative to the code and docs that define them.

It is useful when:

- the repo has changed a lot and you want to know which public artifacts should be regenerated
- the dashboard or summary reports look suspiciously out of date
- you want a lightweight guardrail before pushing public-facing updates
