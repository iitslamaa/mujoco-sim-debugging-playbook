# Bootstrap Environment Guide

Use the bootstrap script to create a local virtual environment, install the repo, and print the key runtime versions.

```bash
bash scripts/bootstrap_env.sh
```

This is useful when validating a fresh machine or reproducing a user issue from a clean local setup.
