# Dependency Map Guide

The dependency map explains how the high-level generated artifacts relate to each other.

It is most useful alongside the regeneration plan:

- the dependency map tells you what an artifact depends on
- the regeneration plan tells you what to rerun first

Together they make stale-output debugging much easier.
