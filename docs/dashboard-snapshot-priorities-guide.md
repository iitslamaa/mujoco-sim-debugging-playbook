# Dashboard Snapshot Priorities Guide

Run the priorities generator to condense the focus summary and resolution plan into a short ranked objective list.

```bash
python scripts/generate_dashboard_snapshot_priorities.py
```

Outputs:

- `outputs/dashboard_snapshots/priorities.json`
- `outputs/dashboard_snapshots/priorities.md`
