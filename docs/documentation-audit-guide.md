# Documentation Audit Guide

The documentation audit is a narrow report focused on self-serve coverage.

It answers two questions:

- how much of the open support queue has knowledge-base coverage
- which known gap targets still have no self-serve article

This is useful when deciding whether to invest in another incident bundle or promote an already-known mitigation into reusable docs.
